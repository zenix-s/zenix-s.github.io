---
const { title, description, url, highlights } = Astro.props;

const formatTechs = (values: string[]) =>
  values.toString().replaceAll(",", " • ");
---

<>
  <a
    class="group flex w-full cursor-pointer flex-col justify-between gap-2 rounded-md border border-neutral-700 p-4 transition-all duration-300 hover:bg-neutral-400/10  hover:border-neutral-400 md:flex-row md:items-center"
    href={url}
    target="_blank"
    rel="noreferrer"
  >
    <div class="flex flex-col gap-4 md:items-start">
      <p class="text-neutral-100">{title}</p>
      <p>{formatTechs(highlights)}</p>
    </div>

    <svg
      width="18"
      height="18"
      viewBox="0 0 18 18"
      fill="none"
      class="transition-all duration-300 text-neutral-500 group-hover:text-neutral-100"
    >
      <path
        d="M5.25 12.75L12.75 5.25"
        stroke="currentColor"
        stroke-linecap="round"
        stroke-linejoin="round"></path>
      <path
        d="M5.25 5.25H12.75V12.75"
        stroke="currentColor"
        stroke-linecap="round"
        stroke-linejoin="round"></path>
    </svg>
  </a>
</>
