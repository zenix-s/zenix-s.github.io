---
import SectionContainer from "@/components/SectionContainer.astro";
import { education } from "@cv";
---

<SectionContainer title="Educación">
  <ul>
    {
      education.map((educ) => (
        <li>
          <h3 class="text-2xl font-bold">{educ.institution}</h3>
          <p class="text-gray-300">
						{educ.startDate} 
						- 
						{
							educ.endDate 
							? educ.endDate 
							: "Actualidad"
						}
					</p>
          <p class="font-medium">{educ.area}</p>
        </li>
      ))
    }
  </ul>
</SectionContainer>
